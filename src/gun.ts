

/**
 * shell
 * power smoke fire
 * warheads
 * hit exposion piecing
 * miss exposion
 */

import { AmmoPool } from "./ammo";
import { Player } from "./player";

export class Gun {
    shoot(
        player: Player,
        ammoPool: AmmoPool
    ) {
    }
}